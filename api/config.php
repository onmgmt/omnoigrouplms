<?php
/**
 * ============================================================
 * Omnoi Academy LMS — ตั้งค่าฐานข้อมูล
 * ============================================================
 * แก้ 4 ค่าด้านล่างให้ตรงกับฐานข้อมูล MySQL ที่สร้างไว้ใน DirectAdmin
 * (ดูวิธีสร้าง DB + หาค่าพวกนี้ได้ในไฟล์ README.md หัวข้อ "Deploy บน HostNeverDie")
 *
 * ⚠️ ไฟล์นี้มีข้อมูลลับ (รหัสผ่าน DB) — .htaccess ในโฟลเดอร์นี้กันไม่ให้เข้าถึงผ่านเว็บโดยตรงแล้ว
 * แต่ควรตรวจสอบว่า Apache/PHP ทำงานตามที่คาดไว้หลัง deploy ด้วย (ดูหัวข้อ "ตรวจสอบความปลอดภัยหลัง deploy" ใน README)
 */

// ---- แก้ 4 บรรทัดนี้ตามค่าจริงจาก DirectAdmin → MySQL Management ก่อน deploy เสมอ ----
define('DB_HOST', 'localhost');
define('DB_NAME', 'CHANGE_ME_db_name');
define('DB_USER', 'CHANGE_ME_db_user');
define('DB_PASS', 'CHANGE_ME_db_password');
// ----------------------------------------------------------------------

// เกณฑ์ผ่านแบบทดสอบ (%) — ใช้ตัดสินว่า "ผ่านคอร์ส" เพื่อปลดล็อกคอร์ส level ถัดไปด้วย
define('PASS_MARK', 80);

// ล็อกบัญชีชั่วคราวหลัง login ผิดติดกันกี่ครั้ง / ล็อกกี่นาที
define('LOGIN_MAX_ATTEMPTS', 8);
define('LOGIN_LOCK_MINUTES', 15);

// เปิด/ปิดการแสดง error โดยละเอียด (ตั้งเป็น false บนเซิร์ฟเวอร์จริงเสมอ)
define('DEBUG_MODE', false);
