-- ============================================================
-- Migration: เพิ่มคอลัมน์ courses.published (สำหรับฟีเจอร์ "ซ่อน/เผยแพร่คอร์ส")
-- รันไฟล์นี้ครั้งเดียวบนฐานข้อมูล production ที่ import schema.sql ไปแล้วก่อนหน้านี้
-- (ถ้าเพิ่งสร้างฐานข้อมูลใหม่จาก schema.sql เวอร์ชันล่าสุด ไม่ต้องรันไฟล์นี้ — คอลัมน์มีอยู่แล้ว)
--
-- วิธีรัน: DirectAdmin → MySQL Management → phpMyAdmin → เลือกฐานข้อมูล → แท็บ SQL → วางคำสั่งด้านล่าง → Go
-- ============================================================

ALTER TABLE courses
  ADD COLUMN published TINYINT(1) NOT NULL DEFAULT 1 COMMENT '0 = ฉบับร่าง/ซ่อนจากพนักงาน, 1 = เผยแพร่แล้ว' AFTER level;

-- คอร์สทุกคอร์สที่มีอยู่แล้วจะถูกตั้งเป็น published = 1 อัตโนมัติ (DEFAULT 1) — พนักงานยังเห็นคอร์สเดิมทั้งหมดเหมือนก่อนอัปเดต ไม่มีอะไรถูกซ่อนโดยไม่ตั้งใจ
